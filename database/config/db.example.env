# Flight Booking System — Database Environment Config
# Copy this file to config/db.env and fill in your values.
# NEVER commit db.env to Git (it is in .gitignore).

DB_HOST=localhost
DB_USER=fb_admin'@'%'
DB_PASS=change_this_password
